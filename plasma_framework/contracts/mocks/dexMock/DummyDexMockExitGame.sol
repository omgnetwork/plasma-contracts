pragma solidity 0.5.11;

/**
 * Since we are not testing the exitability of Dex mock (not really useful to test a mock...)
 * so we just need a dummy contract to pretends there is an exit game for Dex (mock).
 */
contract DummyDexMockExitGame {
}
