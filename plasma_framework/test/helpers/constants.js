const PROTOCOL = {
    MVP: 1,
    MORE_VP: 2,
};

const TX_TYPE = {
    PAYMENT: 1,
};

const OUTPUT_TYPE = {
    PAYMENT: 1,
};

const EMPTY_BYTES = '0x';

module.exports = {
    EMPTY_BYTES,
    PROTOCOL,
    TX_TYPE,
    OUTPUT_TYPE,
};
